import styled from '@emotion/native'

export const OrContainer = styled.View(() => ({
  flexDirection: 'row',
  alignItems: 'center',
  marginVertical: 20,
  width: '100%',
}))

export const Stray = styled.View(() => ({
  flex: 1,
  height: 1,
  backgroundColor: 'grey',
  marginHorizontal: 10,
}))
